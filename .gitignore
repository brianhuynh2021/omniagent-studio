# Python
__pycache__/
*.py[cod]
.venv/
env/
.env

# Node / Web
node_modules/
dist/
.DS_Store

# Legal assistant — never commit case data or secrets.
# The patterns are unanchored because LEGAL_CASE_BANK_PATH can put the database
# anywhere, and the salt is written next to whatever path that resolves to.
backend/.env
*.db
.case_bank_salt
backend/.venv/
