fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.6.0
pydantic-settings>=2.1.0
python-multipart>=0.0.9
requests>=2.31.0
python-dotenv>=1.0.0

# Document extraction / OCR
pypdf>=5.1.0
python-docx>=1.1.2
pytesseract>=0.3.13
Pillow>=11.0.0

# LLM providers (all optional at runtime — the app falls back to
# retrieval-only when no API key is configured)
anthropic>=0.40.0
google-genai>=1.0.0
openai>=1.60.0

# Vector DB / local embedding (optional adapters are lazy-loaded at runtime)
qdrant-client>=1.12.0
sentence-transformers>=3.3.0

# Cloud OCR adapters (optional; credentials are intentionally not bundled)
google-cloud-vision>=3.8.0
boto3>=1.35.0
